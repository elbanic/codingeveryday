namespace parallel_courses {

    function minimumSemesters(n: number, relations: number[][]): number {

        return 0
    };

    const n = 3, relations = [[1, 3], [2, 3], [3, 4]]
    console.log(minimumSemesters(n, relations))
}